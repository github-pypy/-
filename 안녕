안녕
inner 결합 예시 코드
기본 사용법: pd.concat([df1, df2], join='inner')
열 방향 결합: pd.concat([df1, df2], axis=1, join='inner')
예시 데이터프레임
df1 = pd.DataFrame({
    'a': [1, 2, 3],
    'b': [4, 5, 6]
})

df2 = pd.DataFrame({
    'a': [2, 3, 4],
    'b': [5, 6, 7]
})
결과:
result = pd.concat([df1, df2], join='inner')
print(result)
